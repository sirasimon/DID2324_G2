package it.polito.did.g2.shopdrop.navigation

import androidx.navigation.NavController
import androidx.navigation.NavGraphBuilder

fun NavGraphBuilder.cstGraph(navController: NavController){

    /*
    navigation(startDestination = Screens.ScreenLoginRoute.route, route = Screens.CstRoute.route){
        composable(route = ){
            val viewModel = it.sharedViewModel<MainViewModel>(navController)
            //TODO
        }
    }
     */
}